pip install streamlit
pip install pandas
pip install matplotlib
pip install altair
pip install folium
pip install plotly
pip install numpy
pip install streamlit_folium

start XAMPP: Apache and MySQL

open folder in VS Code

open secrets.toml file aand check all credentials are acc to ur phpmyadmin (like check 'database' name ? )

open streamlit_app.py file

run command "streamlit run streamlit_app.py" in terminal

